type: object
properties:
  video:
    type: object
    properties:
      filename: string
  transcripts:
    type: object
    properties:
      filename: string
      diarized: boolean
  context:
    type: object
    properties:
      date: date
      type: string
      topic: string
      session: string
      public: boolean
